# Backend API base URL (no trailing slash). Used for itinerary, places, autocomplete.
# Local: http://localhost:3001
# Production: https://your-backend.onrender.com
VITE_API_URL=

# Google Maps (for the crawl map). Get a key from Google Cloud Console and enable Maps JavaScript API.
# If you see ExpiredKeyMapError, create a new API key.
VITE_GOOGLE_MAPS_API_KEY=
