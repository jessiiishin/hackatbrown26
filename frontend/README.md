
  # Food Crawl Planner App (Copy)

  This is a code bundle for Food Crawl Planner App (Copy). The original project is available at https://www.figma.com/design/rwcLmTJRKbsRoJeG6Ry5e0/Food-Crawl-Planner-App--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  